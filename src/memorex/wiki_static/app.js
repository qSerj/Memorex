const search=document.querySelector('#search');if(search)search.addEventListener('input',()=>{const q=search.value.toLowerCase();document.querySelectorAll('#pages .card').forEach(x=>x.hidden=!x.textContent.toLowerCase().includes(q))});const drop=document.querySelector('.drop');if(drop){drop.addEventListener('dragover',e=>{e.preventDefault();drop.classList.add('active')});drop.addEventListener('drop',()=>drop.classList.remove('active'))}
